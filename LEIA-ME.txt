Tabelaço — projeto completo, estado atual
==========================================

Use este pacote se preferir não caçar arquivo por arquivo. Substituindo
as pastas abaixo, seu projeto fica idêntico ao meu — sem chance de
sobrar arquivo de uma versão antiga (foi isso que quebrou o deploy).

SUBSTITUA:
  src/          (pasta inteira)
  supabase/     (pasta inteira)
  public/
  index.html
  package.json
  package-lock.json
  vite.config.ts
  tsconfig.json
  tsconfig.node.json
  vercel.json

NÃO copie node_modules nem dist — o Vercel gera os dois.

Depois: npm install && npm run build


MIGRATIONS
----------
No Supabase → SQL Editor, na ordem, as que ainda não rodou:

  0026_federated_per_category.sql   (corrigida)
  0027_release_bypass.sql
  0028_team_login_email.sql
  0029_game_rules.sql
  0030_send_off_per_category.sql
  0031_plan_team_limit.sql
  0032_change_plan.sql

Todas são idempotentes: rodar de novo uma que já rodou não faz mal.

Para conferir o banco depois, supabase/tests/ tem a suíte que uso aqui
(precisa de um Postgres local; veja o README de lá).
